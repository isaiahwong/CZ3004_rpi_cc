syntax = "proto3";

service VisionService {
    rpc SendFrame (Request) returns (Response);
}

message Empty{}

message Request {
    bytes image = 1;
    int32 width = 2;
    int32 height = 3;
    int32 channels = 4;
}

message Response {
    int32 count = 1;
}


